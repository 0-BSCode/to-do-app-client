import './App.css';

function App() {
  return (
    <div className="appTest">Jog around the park 3x</div>
  );
}

export default App;


/*

COMPONENT LAYERING
- App (container)
  - Header (title + toggle + add task)
  - Body (task list + task actions + reorder line)
  - Footer (attribution)
*/